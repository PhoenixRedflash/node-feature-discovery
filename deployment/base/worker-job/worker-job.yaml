apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app: nfd
  name: nfd-worker
spec:
  ttlSecondsAfterFinished: 3600
  completions: NUM_NODES
  parallelism: NUM_NODES
  template:
    metadata:
      labels:
        app: nfd-worker
    spec:
      serviceAccountName: nfd-worker
      dnsPolicy: ClusterFirstWithHostNet
      restartPolicy: Never
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - topologyKey: kubernetes.io/hostname
              labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - nfd-worker
      containers:
        - name: nfd-worker
          image: gcr.io/k8s-staging-nfd/node-feature-discovery:master
          imagePullPolicy: Always
          command:
            - "nfd-worker"
          args:
            - "-oneshot"
            # A one-shot worker has no DaemonSet owner. Tie its NodeFeature to
            # the target Node instead of the short-lived Job Pod.
            - "-owner-refs=node"
